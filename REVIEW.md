# REVIEW — PR 审核与知识裁决规范

为什么需要这个文件：知识库的合并是中心化的，但知识的"冲突"大多不是 git 意义上的。两个人给同一栋楼写了不同的到达方案，可能改的是不同文件、不同段落——git 会安静地合并成功，知识层面却是矛盾的。**git 只管文本，这个文件管知识。**

## PR 从哪来

按 `SYNC.md`，PR 由用户的 agent 自动创建：标题、描述、分组提交的格式已经有保证。因此审核者的工作不是重写，而是**验证与裁决**。

## 审核清单（每条 PR 必过）

1. **格式**：描述四段齐全（Added / Modified / Deleted / Sources / Freshness）；所有文件符合 `knowledge/FORMAT.md` 的 frontmatter 要求。
2. **公共性红线**：无个人事务、私人工具链（见 SKILL.md 的公共性红线）。
3. **路由范围**：内容落在正确的路由——学科内容不进 common，全校内容不进学科目录。
4. **来源核验**：Sources 的 URL 抽查可达且能支撑结论。官方数据类事实（时刻/班次/站点）必须与库内官方文档一致；若 PR 声称官方已变更，要求作者附上新公告，并以新核验为准同步更新相关旧文档。
5. **语义冲突扫描**：见下节。

## 语义冲突：真正的难点

三种典型情形：

| 情形 | 例子 | git 表现 |
|---|---|---|
| A. 同文件同段落 | 两人改了方案表的同一行 | 有冲突标记，正常解决 |
| B. 同文件不同段落 | 方案 A 说"步行 5 分钟"，方案 B 说"15 分钟" | **静默合并成功** |
| C. 跨文件 / 跨 PR | PR1 新增 place/x.md 称"X 站下车最近"；PR2 修改 planning.md 称"X 站已不停靠" | **毫无冲突信号** |

### 检测方法

- **实体索引**：审核时从 PR 改动中提取涉及实体——place 文档名、bus 线路与站点名、课程代码、教室缩写。与 main 及**所有 open PR** 的同实体内容比对（不能只和 main 比）。
- **焦点问题**：这个 PR 改动的可过期事实（表格行、时刻、站点）是否与已有内容不同？

### 裁决层级（谁留在文档里）

1. **官方核验数据 > 社区经验**：与 `knowledge/common/bus/` 等官方文档矛盾的个人经验，退回修改，或标注 ⚠️待核 降级保留。
2. **新核验 > 旧核验**：同一事实两个版本都声称官方来源 → `verified.date` 新者留下，旧者随之更新；日期相同 → 在 PR 评论中请作者贴出证据原文再定。
3. **皆为经验且互斥**（如两条路线对用时/可行性说法相反，无法同时为真）→ 评论中请作者按文档里的 freshness.check 复测，5 天无回应则保留先到版本（带 ⚠️ 与日期署名）并开 issue 追踪。
4. **皆为经验且可并存**（两条不同路线都成立）→ 都保留：place 文档的"到达方式总表"本就支持多方案，各标来源与日期。
5. **总原则**：删别人的内容需要更强的证据；加内容只需要合格来源。

## 审核自动化

- **定时任务（每日）**：扫描 open PR → 跑审核清单 → 在 PR 上发结构化审核评论（逐条 ✅/⚠️/❌）并打标签：
  - `ready` — 清单全过、无语义冲突 → **可自动合并**
  - `semantic-conflict` — 命中情形 B/C → 由维护者按裁决层级处理
  - `needs-source` / `needs-format` — 打回给作者修改
- **自动合并的保守边界**：仅 `ready` 且**只新增文件**的 PR（不修改任何既有事实表）；修改既有事实的 PR 一律留给维护者确认。
- **跨 PR 复扫**：合并任意 PR 前重跑一次与其余 open PR 的实体比对，防止合并后产生情形 C。

## 维护者模式

一个人的审核力量总是有限的，欢迎成为 maintainer。职责：

1. 处理 `semantic-conflict` 标签的 PR（按裁决层级）。
2. **季度新鲜度巡检**：对所有 `stable_until` 已过期的文档跑各自的 `freshness.check`——这是知识库不过腐的关键。
3. 裁决升级：自动化任务拿不准的冲突。

**路由方式**：`.github/CODEOWNERS` 按路径指派（`bus/` → 交通维护者、`msc-ai/` → 学科维护者……）。分布式审核的代价是跨范围冲突可能漏检——兜底是每日集中扫描任务，它看得到所有 open PR。

**申请方式**：开 issue 说明你想负责的范围，并附一次合格的示范 PR。
